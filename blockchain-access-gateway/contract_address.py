# Paste the address printed by deploy.js here (exactly as shown, with the 0x).
# You MUST update this every time you restart the Hardhat node (the chain resets).
CONTRACT_ADDRESS = "0xPASTE_DEPLOYED_ADDRESS_HERE"
